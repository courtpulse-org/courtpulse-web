// Demo mode: auth runs against the in-browser store in src/mock. The shapes
// match the NestJS backend's auth/staff/* endpoints.
export { acceptInvite, getMe, login as staffLogin } from "@/mock/api";
